{
  "name": "nurseconnect",
  "version": "1.0.0",
  "description": "NurseConnect - Healthcare Staffing Platform",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build",
    "start": "node server/dist/index.js",
    "dev": "cd server && npm run dev"
  }
}
