services:
  - type: web
    name: nurseconnect
    runtime: node
    plan: free
    buildCommand: cd server && npm install && npm run build
    startCommand: node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
